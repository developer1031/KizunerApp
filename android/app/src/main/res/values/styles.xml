<resources>

  <!-- Base application theme. -->
  <style name="AppTheme" parent="Theme.MaterialComponents.NoActionBar">
    <!-- Customize your theme here. -->
    <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
  </style>

  <style name="BootTheme" parent="Theme.BootSplash.EdgeToEdge">
    <item name="bootSplashBackground">@color/white</item>
    <item name="bootSplashLogo">@null</item>
    <item name="android:windowBackground">@drawable/bootsplash</item>
<!--    <item name="bootSplashBrand">@drawable/bootsplash</item> &lt;!&ndash; Only if you have a brand image &ndash;&gt;-->
    <item name="postBootSplashTheme">@style/AppTheme</item>
  </style>

  <style name="DialogDatePicker.Theme" parent="Theme.AppCompat.Light.Dialog">
    <item name="colorAccent">@color/primary</item>
  </style>
</resources>
